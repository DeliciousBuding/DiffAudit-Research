# Post-Fisher Next-Lane Reselection

Date: 2026-05-11

## Verdict

`selected-cpu-first`; no GPU task is released.

The diagonal-Fisher self-influence stability board closes the current
white-box successor line as `negative-but-useful`. The next Research slot
should not stay in same-score white-box tuning. The highest-value CPU-first
lane is I-A truth hardening: tighten the admitted-evidence interpretation
around finite empirical tails, low-FPR denominators, and bounded adaptive
attacker language.

## Reselection

| Candidate | Decision | Reason |
| --- | --- | --- |
| Larger diagonal-Fisher packet | reject | Stability board ties `raw_grad_l2_sq`; larger same-score run would not test a new hypothesis. |
| Black-box response-contract | hold | Current Kandinsky/Pokemon package is `needs_query_split`; weights-only material cannot supply query images or responses. |
| Gray-box tri-score / ReDiffuse | hold | Both are bounded candidate-only/internal-only and do not have a new scorer hypothesis. |
| I-B defense | hold | Requires defended-shadow training, adaptive attacker, retained utility, and low-FPR gate before reopen. |
| I-C cross-permission | hold | Requires same-spec evaluator and matched comparator. |
| I-A finite-tail / adaptive boundary | select | Uses existing admitted evidence; can harden product-facing interpretation without GPU or schema changes. |

## Next CPU Contract

Run a CPU-only I-A review that checks whether admitted rows and exported
consumer bundles consistently expose:

- finite-tail denominators for low-FPR fields,
- distinction between empirical strict-tail counts and calibrated sub-percent
  FPR claims,
- adaptive-attacker caveats for defended comparators,
- provenance boundaries separating `DDPM/CIFAR10` from conditional diffusion
  or commercial models.

Falsifier: if any public or consumer-facing document presents candidate-only
rows as admitted, omits finite-tail denominators where strict-tail evidence is
used, or overstates defended/adaptive robustness, the review must patch the
contract before any model lane is reopened.

## GPU State

`active_gpu_question = none`; `next_gpu_candidate = none`.
