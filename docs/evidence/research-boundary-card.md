# Research Narrative Limitations Card

1. **Recon**: State that `recon DDIM public-100 step30` is a verified black-box risk line. Input data uses fine-tuned/controlled/public-subset/proxy-shadow-member semantics. This is an active risk proof, not a completed or final version.
2. **PIA baseline**: Reference the workspace-verified + bounded repeated-query adaptive review. The baseline exposes `epsilon-trajectory consistency` on local DDPM/CIFAR10 assets. Report all four metrics: `AUC / ASR / TPR@1%FPR / TPR@0.1%FPR = 0.841339 / 0.786133 / 0.058594 / 0.011719`. `TPR@0.1%FPR` is a finite empirical strict-tail point over 512 target nonmembers, not calibrated sub-percent FPR. Checkpoint/source provenance blocks any paper-aligned release.
3. **PIA defended**: The `stochastic-dropout all-steps prototype` is a provisional defended comparator. It shows inference-time randomization weakening `epsilon-trajectory consistency` on the same local DDPM/CIFAR10 contract. Report all four metrics: `0.828075 / 0.767578 / 0.052734 / 0.009766`. The repeated-query review covers bounded adaptive exploration only. Do not call this reproduced, published, or validated privacy protection.
4. **GSA + DPDM**: These form a verified white-box bridge pair (`GSA 1k-3shadow` attack, `DPDM strong-v3 full-scale` defense) under the same protocol as a frozen comparator. This is a verified bridge stage, not a final benchmark.
5. **Restrictions**: Never claim `SMP-LoRA` is a verified run, a GPU release, or a final attack/defense report. It is a comparator-design exploration, shifted to comparator hypothesis discussion since `2026-04-13`.
6. **Replay artifacts and SMP-LoRA outputs**: These are Runtime recon replay/demo-trace materials, stored in `Research/experiments/...`. They serve demo/debugging/trace purposes only and are not verified evidence.
7. **SMP-LoRA optimizer/lr frontier and batch32 comparator topics**: Place these under "future gate/comparator hypothesis". They require separate review and must not be mixed with verified tracks.
8. **Conditional future-surface**: Currently we have `SD1.5` local canary contract, first bounded `CFG` packet, and first actual runner-level hidden-guidance-jitter rerun = `negative but useful`. There is no honest bounded successor track yet. Do not claim conditional diffusion is fully covered, hidden-guidance jitter is a release-grade defense, or low-FPR / adaptive robustness is established.
9. **Cross-box score sharing**: Current cross-box packets are candidate-only. They show useful AUC movement on aligned surfaces but do not establish stable low-FPR gains. Use [cross-box-boundary-status.md](cross-box-boundary-status.md) for the current boundary.
10. **CLiD prompt-conditioned candidate**: CLiD is strong and repeat-stable under the current prompt-conditioned SD/CelebA bridge, but prompt-neutral perturbation collapses the signal. Treat it as a prompt-conditioned candidate only. Use [clid-prompt-conditioning-boundary.md](clid-prompt-conditioning-boundary.md) for the current boundary and next admission test.
11. **ReDiffuse collaborator baseline**: The collaborator bundle and 750k checkpoint are runnable, but exact replay shows modest AUC and weak strict-tail evidence. Treat ReDiffuse as candidate-only; do not release the 800k shortcut without a new scorer hypothesis and CPU preflight.
12. **Gray-box tri-score**: CDI/TMIA-DM/PIA tri-score aggregation is internal-only positive-but-bounded evidence. It does not replace the admitted PIA row and should not appear as Platform evidence.
13. **Second response-contract portability**: The package contract exists, but the current `Download/black-box` roots do not contain a ready paired query/response package. No black-box portability GPU task is released.

**Summary**: Verify recon, PIA, and GSA-DPDM against their verified main lines. PIA must include mechanistic language, bounded adaptive limitations, and low-FPR metrics. ReDiffuse, tri-score, cross-box fusion, GSA LR, H2/simple-distance, CLiD, response-contract acquisition, SMP-LoRA, and replay artifacts are not admitted product evidence unless a later reviewed promotion explicitly changes that status.
