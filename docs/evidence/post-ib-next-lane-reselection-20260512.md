# Post-I-B Next-Lane Reselection

Date: 2026-05-12

## Verdict

`select-I-C-same-spec-evaluator-feasibility-scout`; no GPU task is released.

After the I-B protocol audit closed as `hold-structural`, the next Research
step must avoid repeating closed same-contract routes. The selected CPU-first
question is whether I-C cross-permission work has enough current execution
surface to define a same-spec evaluator and matched-comparator release board.

## Decision Inputs

| Candidate | Current state | Decision |
| --- | --- | --- |
| I-B risk-targeted unlearning | `hold-structural`; current review borrows undefended shadow threshold transfer. | Do not run. Needs defended-shadow/adaptive protocol first. |
| SecMI consumer row | `structural-support-only`; metrics are strong but product semantics and adaptive comparability are unresolved. | Do not run another metric packet. |
| ReDiffuse | `candidate-only`; exact replay has modest AUC and weak strict-tail evidence. | Do not run 800k or larger packet without a new scorer hypothesis. |
| Mid-frequency same-noise residual | `candidate-stable-but-bounded`; comparator audit narrows claim away from mid-frequency specificity. | Stop same-contract GPU expansion. |
| White-box diagonal-Fisher | `negative-but-useful`; stability board ties `raw_grad_l2_sq`. | Closed unless a genuinely different observable appears. |
| Black-box response-contract package | `needs_query_split`; manifest still lists only manual-intake placeholders for the second response-contract query/response package. | Do not schedule execution until real query/response assets exist. |
| I-C cross-permission | `hold`; same-spec evaluator and matched random comparator remain the stated missing pieces. | Select a CPU-only feasibility scout. |

## Selected Bounded Question

Hypothesis: current I-C code and artifacts may already contain enough structure
to freeze a same-spec evaluator contract without running GPU.

Falsifier: if the active executable surface remains only translated-alias
canaries with `translation_not_same_spec = true`, `same_spec_reuse = false`,
and a `1 member / 1 nonmember` local score-gap readout, then I-C remains hold
and does not get a CPU/GPU release.

Metrics required for any future release board:

- `AUC`
- `ASR`
- `TPR@1%FPR`
- `TPR@0.1%FPR`
- matched random comparator gap
- same member/nonmember identity and same-spec evaluator identity

## GPU State

`active_gpu_question = none`; `next_gpu_candidate = none`.

## Product Boundary

No Platform or Runtime handoff is needed. This is a Research-only selection
step and does not change admitted rows.
