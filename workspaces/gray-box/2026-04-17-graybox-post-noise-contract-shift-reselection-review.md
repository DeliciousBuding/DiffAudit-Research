# 2026-04-17 Gray-Box Post-Noise Contract-Shift Reselection Review

## Question

After both `Noise as a Probe promotion-gap review` and `Noise as a Probe contract-shift review` closed negatively, should gray-box still keep the next live CPU-first slot, or should the main loop now yield priority to another lane?

## Inputs Reviewed

- `D:\Code\DiffAudit\ROADMAP.md`
- `D:\Code\DiffAudit\Research\ROADMAP.md`
- `D:\Code\DiffAudit\Research\workspaces\gray-box\plan.md`
- `D:\Code\DiffAudit\Research\workspaces\gray-box\2026-04-17-noise-as-probe-promotion-gap-review.md`
- `D:\Code\DiffAudit\Research\workspaces\gray-box\2026-04-17-noise-as-probe-contract-shift-review.md`
- `D:\Code\DiffAudit\Research\workspaces\black-box\plan.md`
- `D:\Code\DiffAudit\Research\workspaces\white-box\plan.md`

## Current Gray-Box Truth

What gray-box already has:

- `PIA + stochastic-dropout(all_steps)` as admitted defended headline
- `TMIA-DM late-window + temporal-striding(stride=2)` as strongest defended challenger reference
- `SecMI` as independent corroboration line
- `CDI` as frozen internal collection-level audit extension
- `Noise as a Probe` as strengthened bounded challenger candidate

What gray-box no longer has:

- a clean next `CDI` promotion question
- an honest `Noise as a Probe` promotion path on the current contract
- an honest latent-diffusion same-surface board worth building right now
- any justified new GPU release

## Candidate Review

### 1. Keep gray-box on another immediate latent-diffusion branch

Not selected.

Reason:

- `Noise as a Probe` promotion and contract-shift both already closed negatively;
- `MoFit` remains `current-contract hold`;
- `structural memorization` remains direction-negative;
- continuing to rotate inside the same latent-diffusion pocket would create low-value churn, not a new bounded question.

### 2. Reopen older gray-box DDPM branches

Not selected.

Reason:

- `TMIA-DM` is already packaging-complete for the current round;
- `CDI` is already boundary-complete for the current round;
- `SimA` still lacks a new paper-faithful hypothesis strong enough to outrank other work.

### 3. Yield the next live slot to another lane

Selected.

Reason:

- root-level priorities still say method diversity and second-line depth are insufficient;
- black-box currently remains mostly in wording/asset-hold posture, not in a fresh candidate-generation state;
- white-box explicitly says its near-term work should bias toward `queue-truth sync and new-hypothesis selection`, which is now a better use of CPU budget than another gray-box reselection loop.

## Selection

- `selected_next_live_lane = white-box post-breadth next-hypothesis selection review`

## GPU Posture

- `gpu_release = none`
- `next_gpu_candidate = none`

Gray-box should now be treated as:

- `stable mainline box`
- `no-new-gpu-question`
- `no immediate next-family execution ask`

until a genuinely new gray-box mechanism or contract change appears.

## Verdict

- `graybox_post_noise_contract_shift_reselection_verdict = negative but clarifying`
- gray-box should yield the next live CPU-first slot
- the next honest live lane should move to:
  - `white-box post-breadth next-hypothesis selection review`

## Handoff Decision

- `Research/ROADMAP.md`: update required
- `workspaces/gray-box/plan.md`: update required
- `workspaces/white-box/plan.md`: suggestion-only
- `Platform/Runtime`: no immediate handoff
- `competition materials`: no immediate sync

Reason:

- this round changes lane priority, not current packaged claims.
