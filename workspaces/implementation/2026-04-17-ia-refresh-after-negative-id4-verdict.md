# 2026-04-17 I-A Refresh After Negative I-D.4 Verdict

## Question

After the first actual runner-level `I-D.4` hidden-guidance-jitter rerun closed as `negative but useful`, does the repository need to weaken the current `I-A` innovation posture, or does that negative future-surface result instead reinforce `I-A` as the strongest near-term innovation track?

## Inputs Reviewed

- `<DIFFAUDIT_ROOT>/Research/ROADMAP.md`
- `<DIFFAUDIT_ROOT>/Research/docs/comprehensive-progress.md`
- `<DIFFAUDIT_ROOT>/Research/docs/mainline-narrative.md`
- `<DIFFAUDIT_ROOT>/Research/docs/research-boundary-card.md`
- `<DIFFAUDIT_ROOT>/Research/docs/competition-defense-qa.md`
- `<DIFFAUDIT_ROOT>/Research/docs/competition-innovation-summary.md`
- `<DIFFAUDIT_ROOT>/Research/docs/admitted-results-summary.md`
- `<DIFFAUDIT_ROOT>/Research/workspaces/implementation/2026-04-17-ia-trajectory-consistency-truth-hardening.md`
- `<DIFFAUDIT_ROOT>/Research/workspaces/implementation/2026-04-17-id4-actual-runner-level-hidden-guidance-jitter-verdict.md`

## Review

### 1. `I-D.4` does not weaken `I-A`; it narrows a competing future-surface branch

The negative actual runner-level `I-D.4` rerun means:

- the first deterministic hidden-guidance-jitter contract is not a usable near-term defense path;
- the conditional future-surface remains below low-FPR and adaptive-release honesty;
- `next_gpu_candidate` must stay at `none`.

It does **not** imply:

- that `PIA` stopped exposing a stable gray-box signal;
- that `stochastic-dropout(all_steps)` lost its admitted defended reading;
- that the current `I-A` mechanism or bounded adaptive review became invalid.

So the right project-level reading is:

- `I-D.4` reduces pressure to promote conditional-defense wording;
- `I-A` remains the strongest already-landed innovation track.

### 2. The strongest near-term innovation statement is still the `I-A` mechanistic packet

The current repo still has one innovation line that is both:

- already tied to admitted evidence, and
- already bounded by honest anti-overclaim language.

That line is:

- `PIA` exposes `epsilon-trajectory consistency`;
- `stochastic-dropout(all_steps)` weakens that signal via inference-time randomization;
- the result survives one bounded repeated-query adaptive review;
- higher-layer reporting must carry `AUC / ASR / TPR@1%FPR / TPR@0.1%FPR` together.

This remains stronger than trying to elevate:

- a negative actual conditional-defense rerun;
- a future conditional-defense idea below low-FPR maturity;
- any narrative-only wording upgrade.

### 3. One higher-layer drift was still present

Most higher-layer docs were already aligned, but one stale conditional sentence remained:

- `mainline-narrative.md` still described the bounded hidden-guidance idea as looking promising on average.

After `I-D.4`, the correct sentence is stricter:

- the CPU-side mixed-packet idea was useful for bounded exploration;
- the first actual runner-level deterministic rerun closed `negative but useful`;
- this future-surface line remains below admitted, below release-grade defense, and below automatic GPU release.

## Verdict

- `ia_refresh_after_negative_id4_verdict = positive but stabilizing`

More precise reading:

1. `I-A` remains the strongest near-term innovation track after the failed actual `I-D.4` rerun.
2. The negative conditional rerun strengthens the decision to keep `I-A` above `I-D` in near-term priority order.
3. No admitted gray-box claim changes.
4. One stale higher-layer conditional sentence required correction.

## Frozen Posture

- `active_gpu_question = none`
- `next_gpu_candidate = none`
- `completed_task = I-A truth-hardening refresh after negative actual I-D rerun`
- `next_live_cpu_first_lane = X-15 non-graybox next-lane reselection after I-A refresh`
- `carry_forward_cpu_sidecar = higher-layer PIA provenance / I-A boundary maintenance`

## Handoff Decision

- `Research/ROADMAP.md`: update required
- `docs/mainline-narrative.md`: update required
- `docs/comprehensive-progress.md`: update required
- `root_sync = none`
- `platform_runtime_handoff = none`
- `competition_material_sync = none`

Reason:

- this is a research-priority and wording-stability verdict, not a new consumer-contract change.
