# 2026-04-17 I-A Refresh After Negative I-B.14 Verdict

## Question

After the first actual bounded admitted `I-B.14` packet closed as `negative but useful`, does the repository need to weaken the current `I-A` innovation posture, or does that white-box negative instead reinforce `I-A` as the strongest near-term innovation track?

## Inputs Reviewed

- `<DIFFAUDIT_ROOT>/Research/ROADMAP.md`
- `<DIFFAUDIT_ROOT>/Research/docs/comprehensive-progress.md`
- `<DIFFAUDIT_ROOT>/Research/docs/mainline-narrative.md`
- `<DIFFAUDIT_ROOT>/Research/docs/research-boundary-card.md`
- `<DIFFAUDIT_ROOT>/Research/docs/competition-defense-qa.md`
- `<DIFFAUDIT_ROOT>/Research/docs/competition-innovation-summary.md`
- `<DIFFAUDIT_ROOT>/Research/docs/admitted-results-summary.md`
- `<DIFFAUDIT_ROOT>/Research/docs/leader-research-ready-summary.md`
- `<DIFFAUDIT_ROOT>/Research/workspaces/white-box/2026-04-17-finding-nemo-first-truly-bounded-admitted-intervention-review-verdict.md`
- `<DIFFAUDIT_ROOT>/Research/workspaces/white-box/2026-04-17-finding-nemo-post-first-actual-packet-boundary-review.md`
- `<DIFFAUDIT_ROOT>/Research/workspaces/implementation/2026-04-17-ia-trajectory-consistency-truth-hardening.md`

## Review

### 1. `I-B.14` does not weaken `I-A`; it narrows a competing near-term branch

The first actual bounded admitted `Finding NeMo / I-B` packet now says:

- one real fixed-mask intervention-on/off packet exists;
- locality stayed clean;
- attack-side metrics moved slightly upward rather than downward.

That means:

- `I-B` is now stronger than pure intake or `hold`;
- but it is weaker than a defense-positive near-term innovation claim.

It does **not** imply:

- that `PIA` stopped exposing a stable gray-box signal;
- that `stochastic-dropout(all_steps)` lost its admitted defended reading;
- that the mechanistic `epsilon-trajectory consistency -> inference-time randomization` packet became less honest.

So the project-level reading is:

- `I-B.14` narrows a rival branch;
- `I-A` remains the strongest already-landed near-term innovation packet.

### 2. The strongest near-term innovation statement is still the admitted `I-A` packet

The repo still has one innovation line that is all of the following together:

- admitted-adjacent rather than purely exploratory;
- mechanism-backed rather than narrative-only;
- bounded by adaptive and low-FPR honesty;
- already synchronized into higher-layer wording.

That line is still:

- `PIA` exposes `epsilon-trajectory consistency`;
- `stochastic-dropout(all_steps)` weakens that signal via inference-time randomization;
- the result survives one bounded repeated-query adaptive review;
- higher-layer reading must carry `AUC / ASR / TPR@1%FPR / TPR@0.1%FPR` together.

This remains stronger than trying to elevate:

- a white-box branch that now has one actual falsifier;
- any same-family `Finding NeMo` rescue rerun;
- any future-surface idea still below low-FPR / adaptive honesty.

### 3. One higher-layer white-box sentence was still stale

Most `I-A`-facing higher-layer docs were already aligned.

But one stale sentence remained in `mainline-narrative.md`:

- it still described `Finding NeMo + local memorization + FB-Mem` as `adapter-complete zero-GPU hold`

After `I-B.14` and `I-B.15`, the honest sentence is stricter and sharper:

- the branch is now an `actual bounded falsifier`
- not `zero-GPU hold`
- not defense-positive

This does not change admitted `I-A` metrics.

It matters because the relative priority between `I-A` and `I-B` should now be read from current packet truth rather than from pre-execution hold wording.

## Verdict

- `ia_refresh_after_negative_ib14_verdict = positive but stabilizing`

More precise reading:

1. `I-A` remains the strongest near-term innovation track after the failed actual `I-B.14` packet.
2. The negative white-box packet strengthens the decision to keep `I-A` above `I-B` in near-term priority order.
3. No admitted gray-box claim changes.
4. One stale higher-layer white-box sentence required correction.

## Frozen Posture

- `active_gpu_question = none`
- `next_gpu_candidate = none`
- `completed_task = I-A truth-hardening refresh after first actual negative I-B packet`
- `next_live_cpu_first_lane = X-18 non-graybox next-lane reselection after I-A refresh from negative actual I-B packet`
- `carry_forward_cpu_sidecar = higher-layer PIA provenance / I-A boundary maintenance`

## Handoff Decision

- `Research/ROADMAP.md`: update required
- `docs/mainline-narrative.md`: update required
- `docs/comprehensive-progress.md`: update required
- `workspaces/implementation/challenger-queue.md`: update required
- `root_sync = none`
- `platform_runtime_handoff = none`
- `competition_material_sync = none`

Reason:

- this is a research-priority and wording-stability verdict, not a new consumer-contract change.
