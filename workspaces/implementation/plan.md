# 工程实现方向初始计划

## 方向定位

工程方向负责为黑盒、白盒、灰盒三条线提供共享基础设施。

## 当前优先事项

1. 稳定 `SecMI` 执行层
2. 统一 config 结构
3. 统一 `ready / blocked / dry-run` 输出结构
4. 统一实验结果 schema
5. 为后续白盒 / 灰盒接入预留接口

## 第一周目标

1. 补更深的 runtime probe
2. 明确资产契约
3. 减少对 ad hoc 命令的依赖
4. 让实验状态更容易被脚本和人共同理解
