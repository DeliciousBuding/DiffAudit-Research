# 2026-04-17 X-41 I-C Fresh Bounded Cross-Box Hypothesis Generation After X-40 Expansion

## Question

After `X-40` restored `I-C` only as a fresh-hypothesis surface, what is the first genuinely new bounded cross-box hypothesis worth pursuing, without reopening the old translated-contract packet family?

## Inputs Reviewed

- `<DIFFAUDIT_ROOT>/ROADMAP.md`
- `<DIFFAUDIT_ROOT>/Research/ROADMAP.md`
- `<DIFFAUDIT_ROOT>/Research/docs/comprehensive-progress.md`
- `<DIFFAUDIT_ROOT>/Research/docs/mainline-narrative.md`
- `<DIFFAUDIT_ROOT>/Research/workspaces/implementation/challenger-queue.md`
- `<DIFFAUDIT_ROOT>/Research/workspaces/implementation/2026-04-17-x40-non-graybox-candidate-surface-expansion-after-x39-reselection.md`
- `<DIFFAUDIT_ROOT>/Research/workspaces/implementation/2026-04-17-post-translated-ic-falsifier-next-lane-reselection-review.md`
- `<DIFFAUDIT_ROOT>/Research/workspaces/implementation/2026-04-17-post-translated-ic-falsifier-system-sync-verdict.md`
- `<DIFFAUDIT_ROOT>/Research/workspaces/white-box/2026-04-17-cross-permission-translated-falsifier-review.md`

## Fresh-Hypothesis Review

### 1. Reopen the old translated-contract targeted-vs-random packet

Not selected.

Reason:

1. `I-C.14` already froze that packet family as `translated-contract-only + negative falsifier`;
2. more work on the same frozen pair would be packet churn rather than a new bounded hypothesis;
3. `X-40` explicitly forbids restoring `I-C` by same-pair salvage.

### 2. Promote a black-box corroboration hypothesis immediately

Not selected.

Reason:

1. current `I-C` contract still excludes black-box from first-packet support;
2. no compatible black-box bridge surface is currently frozen;
3. jumping straight to black-box corroboration would skip the missing middle contract.

### 3. Fresh agreement-first cross-box hypothesis on a tiny multi-pair board

Selected.

Hypothesis:

- the next honest `I-C` question should no longer ask whether one translated intervention packet itself proves support;
- instead it should ask whether cross-permission unification appears first as a bounded passive agreement pattern across a tiny matched multi-pair board.

Repository-safe wording:

- if white-box local concentration and gray-box packet-local membership advantage are both measuring the same underlying memorization structure, then on a tiny frozen canonical pairboard they should rank the same objects directionally before any stronger causal-intervention claim is attempted.

Why this counts as genuinely new:

1. it changes the support object from `single translated intervention packet` to `tiny multi-pair agreement board`;
2. it does not require same-spec alias reuse to succeed first;
3. it asks for agreement-before-intervention rather than re-arguing the already-failed translated-falsifier packet.

## Selected Fresh Hypothesis

- `I-C.15 bounded multi-pair agreement-first hypothesis`

Minimal bounded read:

1. freeze one tiny canonical pairboard:
   - `2 members + 2 nonmembers`
2. preserve the current scope:
   - local `DDPM/CIFAR10` overlap surface only
   - CPU-first only
   - no GPU release
3. require one directional agreement question:
   - do white-box local concentration strength and gray-box packet-local membership advantage rank the same frozen objects in the same broad order?

## Selection

- `selected_next_live_lane = X-42 I-C bounded multi-pair agreement-first contract review after X-41 hypothesis generation`

## Verdict

- `x41_ic_fresh_bounded_crossbox_hypothesis_generation_after_x40_expansion = positive`

More precise reading:

1. `I-C` now has one genuinely new bounded hypothesis again;
2. the new hypothesis is agreement-first, not translated-packet salvage;
3. GPU remains correctly idle because the new line is still only CPU-first contract work.

## Frozen Posture

- `active_gpu_question = none`
- `next_gpu_candidate = none`
- `completed_task = X-41 I-C fresh bounded cross-box hypothesis generation after X-40 expansion`
- `next_live_cpu_first_lane = X-42 I-C bounded multi-pair agreement-first contract review after X-41 hypothesis generation`
- `carry_forward_cpu_sidecar = I-A higher-layer boundary maintenance`

## Canonical Evidence Anchor

- `<DIFFAUDIT_ROOT>/Research/workspaces/implementation/2026-04-17-x41-ic-fresh-bounded-crossbox-hypothesis-generation-after-x40-expansion.md`

## Handoff Decision

- `<DIFFAUDIT_ROOT>/Research/ROADMAP.md`: update required
- `<DIFFAUDIT_ROOT>/Research/workspaces/implementation/challenger-queue.md`: update required
- `<DIFFAUDIT_ROOT>/Research/docs/comprehensive-progress.md`: update required
- `<DIFFAUDIT_ROOT>/Research/docs/mainline-narrative.md`: update required
- `<DIFFAUDIT_ROOT>/ROADMAP.md`: update required
- `platform_runtime_handoff = none`
- `competition_material_sync = note-level only`

Reason:

- this changes innovation-lane truth and restores one honest new `I-C` hypothesis, but it still does not alter admitted metrics, schema, or runtime contracts.
