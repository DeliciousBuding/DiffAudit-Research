# Stable Diffusion MIA Reproduction
# Paper: "Towards Black-Box Membership Inference Attack for Diffusion Models" (ICML 2025)
# Tested with Python 3.10

# Core deep learning
torch>=2.0.0
torchvision>=0.15.0

# Diffusion model
diffusers>=0.21.0
transformers>=4.31.0
accelerate>=0.21.0

# Image quality metric (SSIM)
scikit-image>=0.21.0

# Evaluation (AUC, ROC)
scikit-learn>=1.3.0

# Dataset / IO
numpy>=1.24.0
Pillow>=9.5.0
pandas>=2.0.0
requests>=2.28.0

# Config (OmegaConf for coco-2500-random.yaml)
omegaconf>=2.3.0

# Parquet reading (for LAION metadata)
pyarrow>=12.0.0

# Safety backend for diffusers (required by CompVis/stable-diffusion-v1-4)
safetensors>=0.3.1

# Optional: faster parquet alternative
# fastparquet>=2023.4.0
