# Local Role

This directory is the canonical minimal vendored `SecMI` subset used by the current DiffAudit integration.

Use it for:

- current adapter-facing commands
- current in-repo integration and tests

If you need the full upstream repository, upstream config files, or the full original split tree, use:

- `external/SecMI/`
