# X-144: Non-graybox Next-Lane / GPU-Candidate Reselection After X-143

## Question

After `X-143` synchronized the `G1-A / X-90` consumer boundary, is there a fresh bounded GPU candidate that should be released immediately?

## Inputs

- `X-141 / X-142`: `G1-A / X-90` is now two-seed internal auxiliary positive on matched `512 / 512` surfaces.
- `X-143`: active Research consumer surfaces now prevent promoting that result to the admitted gray-box headline.
- GPU health check: GPU is available but already carries desktop memory load, so any release must be host-fit and high-value.
- Read-only sidecar audit: no current candidate clears the GPU-release bar without a new frozen hypothesis and contract.

## Candidate Review

| Candidate | Decision | Reason |
| --- | --- | --- |
| third same-contract `G1-A` seed | `no-go` | No new stability or story-delta hypothesis; would be parameter inertia. |
| larger same-contract `04-H2` review | `no-go` | `H2` already has a minimal `4 / 4` negative-but-useful follow-up; baseline vs defended deltas remain `0.0`. |
| `05-cross-box` rerun | `hold` | The promoted `logistic_2feature` support line is already landed; no fresh shared-surface hypothesis is frozen. |
| `02-gray-box` sidecar | `hold` | `PIA + SimA` has useful sidecar gains but no stable `TPR@0.1%FPR` lift. |
| `03-white-box` activation-subspace / risky-subspace | `select for CPU preflight` | It is the highest-value method-diversity gap, but current activation observability surfaces are explicitly CPU-first and do not yet define a GPU-safe packet. |
| `01-black-box` parked pool | `hold` | No new asset/portability change. |
| `I-D` conditional diffusion | `hold` | Current DDPM/CIFAR10 evidence must not be generalized to conditional diffusion or deployment claims. |

## Verdict

`positive reselection / GPU hold`

There is no honest immediate GPU release after `X-143`. This is not an operational blocker; it is an evidence-quality boundary. The next valuable move is to freeze a new GPU-safe contract rather than consume GPU on a stale same-family repeat.

## Selected Next Lane

`X-145 03-H1 activation-subspace GPU-preflight contract freeze`

Why it wins:

- it addresses the largest remaining white-box method-diversity gap;
- it could produce a genuinely distinct second-family scout if the contract is made honest;
- it is not another gray-box same-family optimization;
- it has existing CPU observability scaffolding, but still needs a separate GPU-safe packet definition before any run.

## Next State

- `active_gpu_question = none`
- `next_gpu_candidate = provisional 03-H1 activation-subspace scout, pending X-145 contract freeze`
- `current_execution_lane = X-145 03-H1 activation-subspace GPU-preflight contract freeze`
- `cpu_sidecar = I-A low-FPR / adaptive-attacker boundary maintenance`
- Platform / Runtime handoff: none
- Materials handoff: none
