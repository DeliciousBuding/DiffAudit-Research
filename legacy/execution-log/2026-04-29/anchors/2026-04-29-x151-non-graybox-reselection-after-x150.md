# X-151: Non-Graybox Next-Lane Reselection After X-150

## Question

After `X-150` closed the same-block cross-layer activation-stability scout as negative-but-useful, is there an honest immediate GPU candidate, or should the next slot move to CPU-first boundary and candidate-surface work?

## Evidence Reviewed

- `G1-A / X-90` is now two-seed internal auxiliary positive after `X-141 / X-142`, but it is not an admitted gray-box headline and does not justify a third same-contract seed.
- `03-H1 activation-subspace` has now run three bounded scouts:
  - `X-145 / X-146`: naive top-delta selector overfit and inverted on larger holdout.
  - `X-148`: validation-regularized mean-profile selector improved the naive baseline but stayed weak.
  - `X-150`: same-block cross-layer stability gate was executable but held out at only `AUC = 0.5625`, below the primary-validation baseline `AUC = 0.625`.
- `04-defense` currently has no honest immediate GPU lever: `H1` scalar changes are negative or bounded, and `H2` minimal `4 / 4` follow-up is negative-but-useful.
- `05-cross-box` remains a promoted support line with `H1/H2 logistic_2feature`; `H4` remains auxiliary/cost-saver only.
- `I-A` remains the strongest near-term innovation surface and must keep low-FPR plus adaptive-attacker wording guarded after negative auxiliary probes.

## Candidate Review

| Candidate | Decision | Reason |
| --- | --- | --- |
| third same-contract `G1-A / X-90` seed | reject | Would only strengthen internal auxiliary evidence without changing consumer boundary. |
| larger same-observable activation-subspace packet | reject | `X-150` already tested the stricter mean-profile stability gate and failed the holdout verdict surface. |
| per-timestep activation-trajectory observable | park as provisional future GPU candidate | This is materially different from mean-profile channel selection, but needs a CPU contract for features, no-leak selection, and kill gate before any run. |
| `04-H1/H2` same-family GPU follow-up | reject | Current family levers are exhausted or negative; no new defended-story delta is frozen. |
| `05-cross-box` same-table rerun | reject | Current promoted read is stable; repeating the same surface is not a GPU-worthy task. |
| `I-A` low-FPR / adaptive-attacker truth-hardening | select as next live CPU lane | It protects the main innovation claim while the GPU slot waits for a genuinely different candidate. |

## Verdict

`positive reselection / GPU hold`

There is no honest immediate GPU release after `X-150`. The next live lane should be `X-152 I-A low-FPR / adaptive-attacker boundary refresh`, while a possible future GPU candidate is only provisional: a per-timestep activation-trajectory scout that must first pass CPU contract freeze.

## Next State

- `active_gpu_question = none`
- `next_gpu_candidate = provisional per-timestep activation-trajectory scout, pending CPU contract freeze`
- `current_execution_lane = X-152 I-A low-FPR / adaptive-attacker boundary refresh`
- `CPU sidecar = candidate contract sketch for per-timestep activation trajectory observable`
- Platform / Runtime handoff: none
- Materials handoff: none
