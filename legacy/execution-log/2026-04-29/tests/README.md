# Archived X-Specific Tests

These files were created to test internals of one-off X-150 and X-154 scripts.
They are retained as evidence of the closed script contracts, but they are not
part of the default active test suite.

If the corresponding logic becomes reusable, move it into `src/diffaudit/` and
create a normal active test under `tests/`.
