# 2026-04-08 Gray-Box Follow-Up: PIA GPU512 Same-Scale Repeat

## Status Panel

- `owner`: `research_leader`
- `updated_at`: `2026-04-08 23:49:31 +08:00`
- `selected_mainline`: `PIA`
- `current_state`: `same-scale GPU512 repeat completed for baseline and stochastic-dropout defense`
- `gpu_usage`: `single GPU, serial`
- `evidence_level`: `runtime-mainline`

## A. Commands Run

Baseline repeat:

```powershell
conda run -n diffaudit-research python -m diffaudit run-pia-runtime-mainline `
  --config tmp/configs/pia-cifar10-graybox-assets.local.yaml `
  --workspace workspaces/gray-box/runs/pia-cifar10-runtime-mainline-20260409-gpu-512-rerun1 `
  --repo-root external/PIA `
  --member-split-root external/PIA/DDPM `
  --device cuda:0 `
  --max-samples 512 `
  --batch-size 8 `
  --provenance-status source-retained-unverified
```

Defense repeat:

```powershell
conda run -n diffaudit-research python -m diffaudit run-pia-runtime-mainline `
  --config tmp/configs/pia-cifar10-graybox-assets.local.yaml `
  --workspace workspaces/gray-box/runs/pia-cifar10-runtime-mainline-dropout-defense-20260409-gpu-512-rerun1 `
  --repo-root external/PIA `
  --member-split-root external/PIA/DDPM `
  --device cuda:0 `
  --max-samples 512 `
  --batch-size 8 `
  --stochastic-dropout-defense `
  --provenance-status source-retained-unverified
```

## B. Repeat Result

- baseline repeat:
  - `auc = 0.841339`
  - `asr = 0.786133`
  - `tpr@1%fpr = 0.058594`
  - `tpr@0.1%fpr = 0.011719`
  - `elapsed_seconds = 61.80132`
- defense repeat:
  - `auc = 0.829269`
  - `asr = 0.770508`
  - `tpr@1%fpr = 0.035156`
  - `tpr@0.1%fpr = 0.009766`
  - `elapsed_seconds = 69.352488`

## C. Interpretation

- The same-scale repeat preserves the same direction already seen in the first `GPU512` pair.
- The defense result again lowers `AUC`, `ASR`, and both low-FPR TPR metrics relative to the paired baseline.
- The gray-box line now has both multi-scale support (`128 / 256 / 512`) and one same-scale repeat at `512`.
- That is enough to keep `stochastic-dropout` as the current `provisional G-1` with materially stronger support than before.
- The repeat file now also confirms that the defended rerun remains below the paired baseline without needing the previously copied placeholder timing.
