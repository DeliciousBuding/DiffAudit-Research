# Legacy Local-API

这里保留的是研究仓历史上的 `Local-API` Python 服务实现，仅用于兼容回溯和旧 trace 对照。

- 活跃执行控制面在当前根级结构中对应 `<DIFFAUDIT_ROOT>/Runtime-Server`
- 如果旧 trace 中出现 `<DIFFAUDIT_ROOT>/Services/Local-API`，把它视为 `Runtime-Server` 的历史前身命名
- 主称已经切换为 `Runtime`
- `Research` 不再提供对外 HTTP 服务入口
