# Configs

This directory is reserved for declarative configuration files:

- `datasets/`: dataset registration and member or non-member split metadata
- `models/`: model checkpoints, adapters, and runtime parameters
- `attacks/`: attack configuration and hyperparameters
- `benchmarks/`: benchmark scenario definitions and metric settings
