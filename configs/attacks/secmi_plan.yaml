task:
  name: secmi-plan
  model_family: diffusion
  access_level: black_box
assets:
  dataset_id: your-dataset-id
  dataset_name: cifar10
  dataset_root: REPLACE_WITH_YOUR_DATASET_ROOT
  model_id: your-model-id
  model_dir: REPLACE_WITH_YOUR_MODEL_DIR
attack:
  method: secmi
  num_samples: 8
  parameters:
    t_sec: 100
    k: 10
    batch_size: 16
report:
  output_dir: experiments/secmi-plan
