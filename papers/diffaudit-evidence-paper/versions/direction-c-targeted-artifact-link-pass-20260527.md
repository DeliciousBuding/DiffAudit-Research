# Direction C Targeted Artifact-Link Pass - 2026-05-27

## Purpose

This pass tests the highest-value next acquisition path identified after the
selected-corpus review: do not run another broad metadata sweep. Instead, chase
already-known metadata-only seeds through omitted artifact-index surfaces.

Seed:

- `fs20260526-arxiv-02`: `Towards More Realistic Membership Inference Attacks on Large Diffusion Models`
- duplicate broader-source row: `bs20260527-openreview-03`
- `fs20260526-arxiv-05`: `Are Diffusion Models Vulnerable to Membership Inference Attacks?`
- `fs20260526-arxiv-04`: `Membership Inference of Diffusion Models`

Checked surfaces:

- Hugging Face dataset card and Dataset Viewer API
- Hugging Face parquet metadata
- Web search for Papers with Code / artifact-index links
- GitHub raw README and repository HTML tree for the official `SecMI` implementation
- GitHub raw README and repository HTML tree for the official `MIDM` implementation
- Raw notebook HEAD metadata for `Example.ipynb`

No PDFs, image payloads, parquet shards, model checkpoints, generated images,
repository clones, notebooks, or pretrained-model payloads were downloaded.

## Result

Three seeds are no longer metadata-only. Each has a distinct public artifact
surface.

### `ta20260527-hf-01`: LAION-MI split artifact

- Dataset: `https://huggingface.co/datasets/antoniaaa/laion_mi`
- Linked paper: `https://arxiv.org/abs/2306.12983`
- Dataset config: `default`
- Splits: `members`, `nonmembers`
- Rows: `13,396` member rows, `26,874` nonmember rows, `40,270` total rows
- Columns: `url`, `caption`
- Files: separate Hugging Face parquet shards for `members` and `nonmembers`
- License shown by the dataset page: MIT

The Dataset Viewer first-row preview confirms row-level URL/caption records for
both member and nonmember splits. This is a public split artifact, not merely a
paper metadata row.

### `ta20260527-github-02`: SecMI official implementation and split tree

- Repository: `https://github.com/jinhaoduan/SecMI`
- Linked paper: `https://arxiv.org/abs/2302.01316`
- README status: official implementation of `Are Diffusion Models Vulnerable to
  Membership Inference Attacks?`
- Public release tree: `README.md`, `main.py`, `secmia.py`, `train.sh`,
  `mia_evals/member_splits`, `mia_evals/measures`, `score`, and
  `stats/cifar10.train.npz`
- Split files visible in the public tree: four `train_ratio0.5` `.npz` files
  under `mia_evals/member_splits`
- README evidence: default training loads splittings stored in
  `mia_evals/member_splits`; the SecMI command exposes `--model_dir`,
  `--dataset_root`, `--dataset`, `--t_sec`, and `--k`

This is a public code-and-split artifact. The pretrained-model link was observed
only as a README link and was not opened or downloaded.

### `ta20260527-github-03`: MIDM official code/protocol artifact

- Repository: `https://github.com/HailongHuPri/MIDM`
- Linked paper: `https://arxiv.org/abs/2301.09956`
- README status: public code for `Membership Inference of Diffusion Models`
  with an ISC 2023 paper link.
- Public release tree: `README.md`, `attacking_diffusion_models`,
  `diffusion_models`, `data_process.py`, attack scripts, and `Example.ipynb`.
- README evidence: `data_process.py` generates `ffhq_1000_idx.npy` as
  member/nonmember indices; `loss_attack.py` outputs loss values of all
  samples; `likelihood_attack.py` outputs likelihood values of all samples; and
  a pre-trained DDPM Google Drive link is documented.

This is a public code/protocol artifact. The split is described as generated by
code rather than observed as an immutable committed split manifest, and the
pretrained-model link was not opened or downloaded.

## Gate Decision

| Row | Target identity | Split semantics | Score/response coverage | Metric provenance | Consumer boundary | Surface delta | Claim-support level |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `ta20260527-hf-01` | Partial: the dataset card and paper identify Stable Diffusion evaluation, but this pass did not inspect checkpoint-level target identity. | Pass: public `members` and `nonmembers` splits expose row-level URL/caption records. | Partial: the dataset exposes member/nonmember rows, not attack scores, model responses, ROC curves, or feature packets. | Fail: no metric JSON, score array, ROC artifact, or replay script was observed. | Partial: useful for artifact inspection and future corpus expansion; insufficient for downstream audit claims. | Pass: distinct artifact surface relative to prior arXiv/OpenReview metadata rows. | L1 artifact inspection |
| `ta20260527-github-02` | Partial: the official implementation and README name the paper and SecMI target workflow, but this pass did not inspect checkpoint hashes or downloaded pretrained models. | Pass: README and public tree expose documented member split files under `mia_evals/member_splits`. | Partial: code and split artifacts exist, but no public row-score array, response cache, or feature packet was observed. | Fail: no metric JSON, ROC artifact, score array, or verifier packet was observed. | Partial: useful for artifact inspection and future corpus expansion; insufficient for downstream audit claims. | Pass: distinct artifact surface relative to the prior arXiv metadata row. | L1 artifact inspection |
| `ta20260527-github-03` | Partial: the official implementation and README name the MIDM paper and target workflow, but this pass did not inspect checkpoint hashes or downloaded pretrained models. | Partial: README describes generated member/nonmember indices in `ffhq_1000_idx.npy`, but no immutable committed split manifest was observed. | Partial: loss and likelihood attack outputs are documented, but no public row-score array, response cache, or feature packet was observed. | Fail: no metric JSON, ROC artifact, score array, or verifier packet was observed. | Partial: useful for artifact inspection and future corpus expansion; insufficient for downstream audit claims. | Pass: distinct artifact surface relative to the prior arXiv metadata row. | L1 artifact inspection |

## Interpretation

This pass changes Direction C's evidence state in a narrow way:

- It proves that targeted artifact-link chasing can recover nonduplicate public
  artifact surfaces from existing metadata-only rows.
- It does not create a new admitted row, replay row, scoreable packet, or metric
  reproduction.
- It must not be pooled with the existing `21`, `17`, or `10` denominators.
- It is a candidate seed for a future larger distinct-surface corpus, not a
  standalone aggregate result.

## Continuation No-Row Checks

After adding the MIDM row, a short continuation pass checked four remaining
metadata-only seeds and found no additional row-worthy public artifact surface.
These checks are stop notes, not corpus rows.

| Seed | Checked surface | Decision |
| --- | --- | --- |
| `fs20260526-arxiv-03` Noise Aggregation | arXiv record plus exact title/GitHub search. | No public target/checkpoint, split, score/response, ROC, metric JSON, or verifier surface observed. |
| `fs20260526-arxiv-09` Privacy Threats in Stable Diffusion Models | arXiv record plus exact title/code search. | No paper-specific public row/metric artifact observed; generic Stable Diffusion model-card links are not paper artifacts. |
| `v1-04` BAF LoRA parameter-space mitigation | arXiv record and code-link search. | The arXiv abstract says code is in supplementary material; no public row-level artifact or repository surface was observed in this pass. |
| `v1-05` Broken Memories | arXiv v3 record and code-link search. | No public prompt, score, response, metric, or verifier packet observed in this pass. |

## Stop Rule

Do not expand this into broad crawling. The next useful acquisition should chase
only existing metadata-only rows and stop unless a surface exposes at least one
of:

- target/checkpoint identity plus split manifests;
- row-score arrays;
- model responses or feature packets;
- ROC/metric JSON;
- verifier scripts tied to row-level artifacts.
